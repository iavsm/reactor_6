<div id="page-content">
    <h1 class="text-center mt-4"><%= @user.username %>'s Profile</h1>
    <%= grvatar_for @user, size: 200 %>

    <h3 class="text-center mt-4"><%= @user.username %>'s Articles</h3>
    <%= render "/users/article" %>
</div>